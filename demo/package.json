{
  "name": "demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm run build && http-server",
    "build": "webpack --mode production --config ../webpack.demo.config.js"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
